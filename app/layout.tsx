// This file only exists to redirect to the localized layout
// All content is handled by app/[locale]/layout.tsx

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}